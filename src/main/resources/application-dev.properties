spring.flyway.locations=classpath:db/migration,classpath:db/migration-dev

jwt.secret=${JWT_SECRET:change-me-in-production-at-least-32-chars}
agent.master-secret=${AGENT_MASTER_SECRET:change-me-in-production}
credentials.encryption-key=${CREDENTIALS_ENCRYPTION_KEY:change-me-32-chars-minimum}

agent.allow-server-registration=true
