spring.flyway.locations=classpath:db/migration,classpath:db/migration-dev

jwt.secret=${JWT_SECRET:change-me-JWT_SECRET-at-least-32-chars}
agent.master-secret=${AGENT_MASTER_SECRET:change-me-AGENT_MASTER_SECRET-at-least-32-chars}
credentials.encryption-key=${CREDENTIALS_ENCRYPTION_KEY:change-me-CREDENTIALS_ENCRYPTION_KEY-at-least-32-chars}

agent.allow-server-registration=true
