# Copy to .env and set strong secrets before deploy.
DB_PASSWORD=change-me-db-password
REDIS_PASSWORD=change-me-redis-password
JWT_SECRET=change-me-to-a-random-string-at-least-32-chars
AGENT_PSK=change-me-agent-psk-min-16-chars

# dev (default) | prod
SPRING_PROFILES_ACTIVE=dev

# Host port for backend in dev overlay (change if 8080 is already in use)
BACKEND_PORT=8080

# Optional prod CORS (comma-separated origins)
CORS_ALLOWED_ORIGINS=
